{
  "name": "ev-tracker",
  "version": "1.1.17",
  "iosBuildNumber": "20",
  "private": true,
  "description": "期待値トラッカー",
  "scripts": {
    "sync:version": "node scripts/sync-version.mjs",
    "build": "npm run sync:version && node scripts/build-web.mjs",
    "cap:sync": "npm run build && npx cap sync",
    "cap:add:ios": "npm run build && npx cap add ios",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
